# NAME MUST MATCH THE DEPLOYED WORKER. It is "tmarsi", not the "forex-setup-alerts"
# this file used to say - the worker was created under a different name than the
# config assumed. Deploying under the old name would have stood up a SECOND worker
# and left the original running, so two bots would have been alerting at once, the
# old one still on the superseded strategy.
name = "tmarsi"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Two passes per 5-minute candle. ORDER MATTERS: src/index.js identifies which pass
# fired by matching event.cron against the first string here, so the early pass must
# stay first.
#   :03 etc - EARLY, 2 minutes before the bar closes, evaluates the forming bar and
#             sends a "get ready" heads-up.
#   :00 etc - CLOSE, evaluates the bar that just closed, but only for pairs the early
#             pass flagged, so it costs API calls only when something actually formed.
[triggers]
crons = [
  "3,8,13,18,23,28,33,38,43,48,53,58 * * * *",
  "0,5,10,15,20,25,30,35,40,45,50,55 * * * *",
]

# The live namespace this worker already uses. A KV namespace id is an identifier,
# not a credential - it is useless without account auth, which is why Cloudflare's own
# workflow has it committed here. The SECRETS (bot token, chat id, API keys) are set
# separately via the dashboard or `wrangler secret put` and never appear in this file.
#
# This is load-bearing, not optional: KV dedupes alerts per bar, enforces one signal
# per day, carries the pending list from the early pass to the close pass, and counts
# the daily API quota. Deploying with a wrong or placeholder id breaks all four.
[[kv_namespaces]]
binding = "ALERT_STATE"
id = "68f5be9ec3a54af2b7bbcbde75d7a9f1"

# SECRETS ARE DELIBERATELY NOT DECLARED IN THIS FILE.
#
# Cloudflare has two kinds, and only one of them survives this deploy model:
#
#   Plain Worker secrets - set once in the dashboard (Variables and Secrets -> add,
#     type Secret) or via `wrangler secret put`. They live ON the worker, are not
#     config, and a deploy does not touch them. `env.NAME` is a plain string.
#
#   Secrets Store bindings - account-level entries the worker is LINKED to. The link
#     is config. A deploy applies this file wholesale, so any deploy from a config
#     that omits them removes the links.
#
# This worker started on Secrets Store, and the first Git deploy silently unbound all
# four - every request returned Unauthorized because env.WEBHOOK_SECRET had become
# undefined, while the values themselves sat untouched in the account-level store.
# Declaring [[secrets_store_secrets]] here to restore the links did not work either;
# /?health=1 still reported every binding false after a successful deploy.
#
# Rather than keep debugging a binding mechanism that is fighting the deploy model,
# the four secrets are now plain Worker secrets. They cannot be removed by a deploy,
# they need nothing in this file, and there is no store id to keep in sync. The
# one-time cost was re-entering four values; the recurring cost is zero.
#
# resolveSecret() in src/index.js still accepts both shapes, so either kind works.
